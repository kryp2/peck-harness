Fix the pagination bug in this small Python project.

Requirements:

- `page_number` is 1-indexed.
- Preserve the function signature and validation behavior.
- Make the smallest reasonable code change.
- Run the existing tests before finishing.
